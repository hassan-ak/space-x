// React Imports
import React from 'react';
// Routers Imports
import { Outlet } from 'react-router-dom';

// Products Catagory
export const LaunchCat = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
}